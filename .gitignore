# Binaries for programs and plugins
/mumble_bot
/mumble_phone
cmd/mumble_bot/mumble_bot
cmd/mumble_phone/mumble_phone
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out
